import universalMethod from './index.js';

export default function calculator() {
  const CONDITION = 'What is the result of the expression?';
  const nameGame = 'calculator';
  universalMethod(nameGame, CONDITION);
}
